import { StoragePlugin } from '@teliagen/plugin-storage';

export const storagePlugin = StoragePlugin({
defaultDisk: 'local',

disks: {
local: {
driver: 'local',
config: {
baseDir: './uploads',
publicUrl: process.env.STORAGE_LOCAL_URL || 'http://localhost:3000/uploads'
}
},
// Add other disks (s3, minio) here if needed
}
});